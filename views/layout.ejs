<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= typeof title !=='undefined' ? title : 'لوحة التحكم' %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&family=Tajawal:wght@300;400;500;700&display=swap"
    rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    :root {
      --brand-primary: #0ea5e9;
      --brand-primary-light: #e0f2fe;
      --brand-primary-dark: #0369a1;
      --brand-secondary: #64748b;
      --brand-bg: #f8fafc;
      --brand-sidebar-bg: #0f172a;
      --brand-card-bg: #ffffff;
      --brand-text-main: #1e293b;
      --brand-text-muted: #64748b;
      --brand-border: #e2e8f0;
      --brand-success: #10b981;
      --brand-warning: #f59e0b;
      --brand-danger: #ef4444;

      --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
      --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
      --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
      --radius-lg: 1rem;
      --radius-md: 0.75rem;
    }

    body {
      font-family: 'Tajawal', 'Outfit', system-ui, -apple-system, sans-serif;
      background-color: var(--brand-bg);
      color: var(--brand-text-main);
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* Sidebar Styling */
    .sidebar {
      background: var(--brand-sidebar-bg);
      width: 280px;
      transition: all 0.3s ease;
      z-index: 1000;
      border-left: 1px solid rgba(255, 255, 255, 0.05);
    }

    .sidebar-header {
      padding: 2rem 1.5rem;
      background: rgba(255, 255, 255, 0.02);
    }

    .sidebar .nav-link {
      color: #94a3b8;
      padding: 0.875rem 1.5rem;
      margin: 0.25rem 1rem;
      border-radius: var(--radius-md);
      display: flex;
      align-items: center;
      transition: all 0.2s ease;
      font-weight: 500;
    }

    .sidebar .nav-link:hover {
      color: #fff;
      background: rgba(255, 255, 255, 0.05);
    }

    .sidebar .nav-link.active {
      color: #fff;
      background: var(--brand-primary);
      box-shadow: 0 4px 12px rgba(14, 165, 233, 0.3);
    }

    .sidebar .nav-link i {
      font-size: 1.25rem;
      margin-left: 1rem;
    }

    /* Main Content */
    main {
      margin-right: 280px;
      padding: 2rem;
      transition: all 0.3s ease;
    }

    /* Premium Cards */
    .card {
      border: none !important;
      border-radius: var(--radius-lg) !important;
      box-shadow: var(--shadow-sm);
      transition: all 0.3s ease;
      background: var(--brand-card-bg);
    }

    .card:hover {
      box-shadow: var(--shadow-md);
    }

    .stat-card {
      position: relative;
      overflow: hidden;
    }

    .stat-card-icon {
      position: absolute;
      top: 1.5rem;
      left: 1.5rem;
      width: 48px;
      height: 48px;
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      background: var(--brand-primary-light);
      color: var(--brand-primary);
    }

    .stat-card h6 {
      color: var(--brand-text-muted);
      font-weight: 600;
      margin-bottom: 0.5rem;
    }

    .stat-card h3 {
      font-weight: 700;
      margin-bottom: 0;
      color: var(--brand-text-main);
    }

    /* Badges & UI Elements */
    .badge {
      padding: 0.5em 0.8em;
      font-weight: 600;
      border-radius: 6px;
    }

    .table thead th {
      background: var(--brand-bg);
      color: var(--brand-text-muted);
      font-weight: 600;
      text-transform: uppercase;
      font-size: 0.75rem;
      letter-spacing: 0.05em;
      border: none;
      padding: 1rem;
    }

    .table tbody td {
      padding: 1rem;
      vertical-align: middle;
      color: var(--brand-text-main);
      border-bottom: 1px solid var(--brand-border);
    }

    .btn {
      padding: 0.6rem 1.25rem;
      border-radius: var(--radius-md);
      font-weight: 600;
      transition: all 0.2s ease;
    }

    .btn-primary {
      background: var(--brand-primary);
      border: none;
    }

    .btn-primary:hover {
      background: var(--brand-primary-dark);
      transform: translateY(-1px);
    }

    </head><body><% if (typeof adminUsername !=='undefined' && adminUsername) {
      %><div class="d-flex"><nav class="sidebar position-fixed top-0 start-0 bottom-0 shadow-lg"><div class="sidebar-header border-bottom border-white border-opacity-10 text-center"><h4 class="text-white fw-bold mb-1">لوحة التحكم</h4><div class="d-flex align-items-center justify-content-center"><span class="badge bg-success bg-opacity-25 text-success small">متصل</span><small class="text-secondary ms-2 opacity-75"><%=adminUsername %></small></div></div><ul class="nav flex-column pt-3"><li><a class="nav-link <%= typeof title !== 'undefined' && title === 'الرئيسية' ? 'active' : '' %>" href="/admin/dashboard"><i class="bi bi-grid-1x2"></i>الرئيسية</a></li><li><a class="nav-link <%= typeof title !== 'undefined' && title === 'المنتجات' ? 'active' : '' %>" href="/admin/products"><i class="bi bi-box-seam"></i>المنتجات</a></li><li><a class="nav-link <%= typeof title !== 'undefined' && title === 'الطلبات' ? 'active' : '' %>" href="/admin/orders"><i class="bi bi-cart-check"></i>الطلبات</a></li><li><a class="nav-link <%= typeof title !== 'undefined' && title === 'العملاء' ? 'active' : '' %>" href="/admin/customers"><i class="bi bi-people"></i>العملاء</a></li><li><a class="nav-link <%= typeof title !== 'undefined' && title === 'المبيعات' ? 'active' : '' %>" href="/admin/sales"><i class="bi bi-graph-up-arrow"></i>المبيعات</a></li><li><a class="nav-link <%= typeof title !== 'undefined' && title === 'التصنيفات' ? 'active' : '' %>" href="/admin/categories"><i class="bi bi-tags"></i>التصنيفات</a></li><li class="mt-auto mb-3"><a class="nav-link text-danger opacity-75" href="/admin/logout"><i class="bi bi-box-arrow-right"></i>تسجيل الخروج</a></li></ul></nav><main class="flex-grow-1"><%
    }

    %><%- body %><% if (typeof adminUsername !=='undefined' && adminUsername) {
      %></main></div><%
    }

    %><script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script><%- typeof scripts !=='undefined' ? scripts : '' %></body></html>