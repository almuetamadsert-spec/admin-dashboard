<%- include('../partials/head', { title: 'واجهة المتجر — إعلانات السلايدر' }) %>
  <%- include('../partials/sidebar', { adminUsername }) %>

    <h4 class="mb-4">واجهة المتجر — إعلانات السلايدر</h4>
    <p class="text-muted small mb-4">الصور تظهر في أعلى تطبيق المتجر (هاتف/تابلت). أبعاد مناسبة: عرض 1200×400 تقريباً أو
      نسبة 3:1.</p>

    <% if (locals.query && query.error==='no_image' ) { %>
      <div class="alert alert-warning">اختر صورة عند الإضافة.</div>
      <% } %>

        <!-- إعداد وقت تحرك الصور وشكل شبكة البطاقات -->
        <div class="card mb-4" style="position:relative; z-index: 2; overflow: visible;">
          <div class="card-body" style="overflow: visible;">
            <h6 class="card-title mb-3"><i class="bi bi-gear"></i> تصميم بطاقة المنتج</h6>
            <form method="post" action="/admin/store-ui/settings" class="row g-3 align-items-end">
              <input type="hidden" name="interval" value="<%= interval %>">
              <div class="col-auto">
                <label class="form-label small">شكل شبكة بطاقات المنتجات</label>
                <select name="product_layout" class="form-select" style="width:auto">
                  <option value="grid_2" <%=(locals.productLayout || 'grid_2' )==='grid_2' ? 'selected' : '' %>>صفان
                    أفقي (2 بطاقات)</option>
                  <option value="grid_3" <%=(locals.productLayout || 'grid_2' )==='grid_3' ? 'selected' : '' %>>3 بطاقات
                    أفقية</option>
                  <option value="slider" <%=(locals.productLayout || 'grid_2' )==='slider' ? 'selected' : '' %>>سلايدر
                    أفقي</option>
                </select>
              </div>
              <div class="w-100"></div>
              <div class="col-12 mt-2">
                <h6 class="small text-muted mb-2">تعديل بطاقة المنتج</h6>
              </div>
              <div class="col-md-3">
                <label class="form-label small">لون خلفية البطاقة</label>
                <input type="color" name="product_card_bg" class="form-control form-control-color"
                  value="<%= (locals.cardBg || '#ffffff') %>" title="اختر لون الخلفية">
              </div>
              <div class="col-md-3">
                <label class="form-label small">زوايا البطاقة</label>
                <select name="product_card_radius" class="form-select">
                  <% cornerStyles.forEach(function(s) { %>
                    <option value="<%= s.value %>" <%=(locals.cardRadius || 'rounded' )===s.value ? 'selected' : '' %>>
                      <%= s.label %>
                    </option>
                    <% }); %>
                </select>
              </div>
              <div class="col-md-4">
                <div class="form-check mt-3">
                  <input class="form-check-input" type="checkbox" name="product_card_show_add_to_cart" value="1"
                    id="cardShowAdd" <%=locals.showAddToCart ? 'checked' : '' %>>
                  <label class="form-check-label small" for="cardShowAdd">إظهار زر إضافة للسلة</label>
                </div>
                <div class="form-check mt-3">
                  <input class="form-check-input" type="checkbox" name="product_card_show_buy_now" value="1"
                    id="cardShowBuyNow" <%=locals.showBuyNow ? 'checked' : '' %>>
                  <label class="form-check-label small" for="cardShowBuyNow">إظهار زر اشتري الآن</label>
                </div>
              </div>
              <div class="col-md-5">
                <label class="form-label small">إظهار حالة المخزون</label>
                <select name="product_card_stock_style" class="form-select" style="min-width:180px">
                  <option value="none" <%=(locals.stockStyle==='none' || !locals.stockStyle) ? 'selected' : '' %>>إخفاء
                  </option>
                  <option value="text" <%=(locals.stockStyle==='text' ) ? 'selected' : '' %>>إظهار كـ (متوفر / نفد)
                  </option>
                  <option value="number" <%=(locals.stockStyle==='number' ) ? 'selected' : '' %>>إظهار الكمية برقم
                  </option>
                </select>
              </div>
              <div class="col-md-3">
                <label class="form-label small">لون حالة المخزون (متوفر)</label>
                <input type="color" name="product_card_stock_color_av" class="form-control form-control-color"
                  value="<%= (locals.stockColorAv || '#4caf50') %>" title="لون نص متوفر">
              </div>
              <div class="col-md-3">
                <label class="form-label small">لون حالة المخزون (نفد)</label>
                <input type="color" name="product_card_stock_color_out" class="form-control form-control-color"
                  value="<%= (locals.stockColorOut || '#f44336') %>" title="لون نص نفد">
              </div>
              <div class="col-md-3">
                <label class="form-label small">لون السعر</label>
                <input type="color" name="product_card_price_color" class="form-control form-control-color"
                  value="<%= (locals.priceColor || '#0ea5e9') %>" title="لون نص السعر">
              </div>
              <div class="w-100"></div>
              <div class="col-md-3">
                <label class="form-label small">مكان البراند (الشركة) عمودياً</label>
                <select name="card_brand_position" class="form-select" style="min-width:120px">
                  <option value="left" <%=(locals.cardBrandPos || 'left' )==='left' ? 'selected' : '' %>>يسار (بجانب
                    الصورة)</option>
                  <option value="right" <%=(locals.cardBrandPos || 'left' )==='right' ? 'selected' : '' %>>يمين (بجانب
                    الصورة)</option>
                  <option value="top" <%=(locals.cardBrandPos || 'left' )==='top' ? 'selected' : '' %>>فوق الاسم
                  </option>
                  <option value="bottom" <%=(locals.cardBrandPos || 'left' )==='bottom' ? 'selected' : '' %>>تحت السعر
                  </option>
                </select>
              </div>
              <div class="col-md-3">
                <label class="form-label small">محاذاة البراند أفقياً</label>
                <select name="card_brand_align" class="form-select" style="min-width:120px">
                  <option value="left" <%=(locals.cardBrandAlign || 'right' )==='left' ? 'selected' : '' %>>يسار
                  </option>
                  <option value="center" <%=(locals.cardBrandAlign || 'right' )==='center' ? 'selected' : '' %>>وسط
                  </option>
                  <option value="right" <%=(locals.cardBrandAlign || 'right' )==='right' ? 'selected' : '' %>>يمين
                  </option>
                </select>
              </div>
              <div class="col-md-3">
                <label class="form-label small">محاذاة اسم المنتج</label>
                <select name="card_name_align" class="form-select" style="min-width:120px">
                  <option value="left" <%=(locals.cardNameAlign || 'right' )==='left' ? 'selected' : '' %>>يسار</option>
                  <option value="center" <%=(locals.cardNameAlign || 'right' )==='center' ? 'selected' : '' %>>وسط
                  </option>
                  <option value="right" <%=(locals.cardNameAlign || 'right' )==='right' ? 'selected' : '' %>>يمين
                  </option>
                </select>
              </div>
              <div class="col-md-3">
                <label class="form-label small">محاذاة السعر</label>
                <select name="card_price_align" class="form-select" style="min-width:120px">
                  <option value="left" <%=(locals.cardPriceAlign || 'right' )==='left' ? 'selected' : '' %>>يسار
                  </option>
                  <option value="center" <%=(locals.cardPriceAlign || 'right' )==='center' ? 'selected' : '' %>>وسط
                  </option>
                  <option value="right" <%=(locals.cardPriceAlign || 'right' )==='right' ? 'selected' : '' %>>يمين
                  </option>
                </select>
              </div>
              <div class="w-100"></div>
              <div class="col-md-3">
                <label class="form-label small">شكل زر أضف إلى السلة</label>
                <select name="card_add_btn_style" class="form-select" style="min-width:180px">
                  <option value="small_rounded" <%=(locals.cardAddBtnStyle || 'small_rounded' )==='small_rounded'
                    ? 'selected' : '' %>>زر صغير زوايا دائرية</option>
                  <option value="full_rounded" <%=(locals.cardAddBtnStyle || 'small_rounded' )==='full_rounded'
                    ? 'selected' : '' %>>زر كامل دائري</option>
                  <option value="sharp" <%=(locals.cardAddBtnStyle || 'small_rounded' )==='sharp' ? 'selected' : '' %>
                    >زر زوايا حادة</option>
                </select>
              </div>
              <div class="col-md-3">
                <label class="form-label small">موقع الزر في البطاقة</label>
                <select name="card_add_btn_position" class="form-select">
                  <option value="left" <%=(locals.cardAddBtnPosition || 'right' )==='left' ? 'selected' : '' %>>يسار
                  </option>
                  <option value="right" <%=(locals.cardAddBtnPosition || 'right' )==='right' ? 'selected' : '' %>>يمين
                  </option>
                </select>
              </div>
              <div class="col-md-3">
                <label class="form-label small">لون زر أضف إلى السلة</label>
                <input type="color" name="card_add_btn_color" class="form-control form-control-color"
                  value="<%= (locals.cardAddBtnColor || '#06A3E7') %>" title="لون الزر">
              </div>
              <div class="col-md-3">
                <label class="form-label small">لون زر اشتري الآن</label>
                <input type="color" name="card_buy_now_color" class="form-control form-control-color"
                  value="<%= (locals.cardBuyNowColor || '#ff9800') %>" title="لون رز اشتري الآن">
              </div>
              <div class="col-auto mt-4">
                <button type="submit" class="btn btn-primary">حفظ</button>
              </div>
            </form>
          </div>
        </div>

        <!-- تصميم بطاقة المنتج للتاجر (تطبيق التاجر) -->
        <div class="card mb-4">
          <div class="card-body">
            <h6 class="card-title mb-3"><i class="bi bi-palette"></i> تصميم بطاقة المنتج للتاجر</h6>
            <p class="text-muted small mb-3">إعدادات شكل بطاقة المنتج في تطبيق التاجر (تبويب المخزون وغيرها).</p>
            <form method="post" action="/admin/store-ui/settings" class="row g-3 align-items-end">
              <div class="col-md-3">
                <label class="form-label small">لون خلفية البطاقة</label>
                <input type="color" name="merchant_product_card_bg" class="form-control form-control-color"
                  value="<%= (locals.merchantCardBg || '#ffffff') %>" title="لون خلفية بطاقة المنتج للتاجر">
              </div>
              <div class="col-md-3">
                <label class="form-label small">شكل البطاقة (الزوايا)</label>
                <select name="merchant_product_card_radius" class="form-select">
                  <% cornerStyles.forEach(function(s) { %>
                    <option value="<%= s.value %>" <%=(locals.merchantCardRadius || 'rounded' )===s.value ? 'selected'
                      : '' %>><%= s.label %>
                    </option>
                    <% }); %>
                </select>
              </div>
              <div class="col-auto">
                <button type="submit" class="btn btn-primary">حفظ</button>
              </div>
            </form>
          </div>
        </div>

        <!-- إعداد شعار المتجر -->
        <div class="card mb-4">
          <div class="card-body">
            <h6 class="card-title mb-3"><i class="bi bi-image"></i> شعار المتجر</h6>
            <form method="post" action="/admin/store-ui/logo" enctype="multipart/form-data">
              <div class="row g-4 align-items-start">
                <div class="col-md-5">
                  <div class="p-3 border rounded bg-light mb-3">
                    <label class="form-label d-block fw-bold mb-2"><i class="bi bi-upload"></i> تحديث صورة
                      الشعار</label>
                    <input type="file" name="logo" class="form-control mb-2"
                      accept="image/jpeg,image/png,image/gif,image/webp,image/svg+xml">
                    <% if (locals.logoUrl) { %>
                      <div class="mt-2 text-center p-3 border rounded white-bg" style="background: white;">
                        <img src="/uploads/<%= logoUrl %>" alt="شعار المتجر" style="max-height: 80px; width: auto;">
                        <div class="small text-muted mt-2">الشعار الحالي</div>
                      </div>
                      <% } %>
                  </div>
                </div>

                <div class="col-md-7">
                  <div class="row g-3">
                    <!-- الحجم والموقع -->
                    <div class="col-md-6">
                      <label class="form-label small fw-bold">حجم الشعار (%)</label>
                      <div class="d-flex align-items-center gap-2">
                        <input type="range" name="logo_size" class="form-range flex-grow-1" min="10" max="300" step="1"
                          value="<%= (locals.logoSize || '100') %>"
                          oninput="this.nextElementSibling.value = this.value">
                        <output class="badge bg-primary" style="min-width: 45px;">
                          <%= (locals.logoSize || '100' ) %>
                        </output>
                      </div>
                    </div>
                    <div class="col-md-6">
                      <label class="form-label small fw-bold">موقع الشعار</label>
                      <select name="logo_position" class="form-select">
                        <% logoPositions.forEach(function(p) { %>
                          <option value="<%= p.value %>" <%=(locals.logoPosition===p.value) ? 'selected' : '' %>>
                            <%= p.label %>
                          </option>
                          <% }); %>
                      </select>
                    </div>

                    <!-- الهوامش -->
                    <div class="col-12 mt-3">
                      <label class="form-label small fw-bold text-primary"><i class="bi bi-arrows-move"></i> التحكم
                        الدقيق في الموقع (الهوامش بالبكسل)</label>
                      <div class="row g-2">
                        <div class="col-6">
                          <div class="input-group input-group-sm">
                            <span class="input-group-text">أعلى</span>
                            <input type="number" name="logo_margin_top" class="form-control"
                              value="<%= (locals.logoMarginTop || '0') %>">
                          </div>
                        </div>
                        <div class="col-6">
                          <div class="input-group input-group-sm">
                            <span class="input-group-text">أسفل</span>
                            <input type="number" name="logo_margin_bottom" class="form-control"
                              value="<%= (locals.logoMarginBottom || '0') %>">
                          </div>
                        </div>
                        <div class="col-6">
                          <div class="input-group input-group-sm">
                            <span class="input-group-text">يسار</span>
                            <input type="number" name="logo_margin_left" class="form-control"
                              value="<%= (locals.logoMarginLeft || '0') %>">
                          </div>
                        </div>
                        <div class="col-6">
                          <div class="input-group input-group-sm">
                            <span class="input-group-text">يمين</span>
                            <input type="number" name="logo_margin_right" class="form-control"
                              value="<%= (locals.logoMarginRight || '0') %>">
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>

                <div class="col-12 text-end">
                  <hr>
                  <button type="submit" class="btn btn-primary px-4">
                    <i class="bi bi-check2-circle"></i> حفظ إعدادات الشعار بالكامل
                  </button>
                </div>
              </div>
            </form>
          </div>
        </div>

        <!-- إضافة شريحة جديدة -->
        <div class="card mb-4">
          <div class="card-body">
            <h6 class="card-title mb-3"><i class="bi bi-plus-circle"></i> إضافة صورة إعلان</h6>
            <form method="post" action="/admin/store-ui/settings" class="row g-3 align-items-end mb-4">
              <div class="col-auto">
                <label class="form-label small">وقت تحرك صور السلايدر (ثانية، 2–60)</label>
                <input type="number" name="interval" class="form-control" style="width:100px" min="2" max="60"
                  value="<%= interval %>" required>
              </div>
              <div class="col-auto">
                <button type="submit" class="btn btn-outline-primary">حفظ</button>
              </div>
            </form>
            <form method="post" action="/admin/store-ui/slide" enctype="multipart/form-data"
              class="row g-3 align-items-end">
              <div class="col-md-4">
                <label class="form-label small">الصورة</label>
                <input type="file" name="image" class="form-control" accept="image/jpeg,image/png,image/gif,image/webp"
                  required>
              </div>
              <div class="col-md-3">
                <label class="form-label small">شكل الزوايا</label>
                <select name="corner_style" class="form-select">
                  <% cornerStyles.forEach(function(s) { %>
                    <option value="<%= s.value %>">
                      <%= s.label %>
                    </option>
                    <% }); %>
                </select>
              </div>
              <div class="col-md-2">
                <button type="submit" class="btn btn-success w-100">إضافة</button>
              </div>
            </form>
          </div>
        </div>

        <!-- قائمة الشرائح -->
        <div class="card">
          <div class="card-header d-flex justify-content-between align-items-center">
            <span>شرائح الإعلانات (<%= slides.length %>)</span>
          </div>
          <div class="card-body">
            <% if (slides.length===0) { %>
              <p class="text-muted mb-0">لا توجد شرائح. أضف صورة أعلاه.</p>
              <% } else { %>
                <div class="row g-3">
                  <% slides.forEach(function(s, idx) { %>
                    <div class="col-md-6 col-lg-4">
                      <div class="card h-100">
                        <img src="/uploads/<%= s.image_path %>" class="card-img-top" alt="شريحة <%= s.id %>"
                          style="height:140px;object-fit:cover;border-radius:var(--radius, 8px)">
                        <div class="card-body py-2">
                          <p class="small mb-1"><strong>الزوايا:</strong>
                            <%= (cornerStyles.find(function(c){ return c.value===s.corner_style; }) || { label:
                              s.corner_style }).label %>
                          </p>
                          <p class="small text-muted mb-2">الترتيب: <%= idx + 1 %>
                          </p>
                          <div class="d-flex gap-1">
                            <a href="/admin/store-ui/slide/edit/<%= s.id %>"
                              class="btn btn-sm btn-outline-primary">تعديل</a>
                            <form action="/admin/store-ui/slide/delete/<%= s.id %>" method="post" class="d-inline"
                              onsubmit="return confirm('حذف هذه الشريحة؟');">
                              <button type="submit" class="btn btn-sm btn-outline-danger">حذف</button>
                            </form>
                          </div>
                        </div>
                      </div>
                    </div>
                    <% }); %>
                </div>
                <% } %>
          </div>
        </div>

        <%- include('../partials/scripts') %>