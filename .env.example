# ——— محلياً: لا تحتاج تعيين هذه؛ التطبيق يستخدم SQLite (ملف data/shop.db)
# ——— على السحابة: انسخ هذا الملف إلى .env وعدّل القيم (يرجى مراجعة docs/DEPLOY.md)

# إنتاج — يُفضّل تعيينها على السيرفر
# NODE_ENV=production
# PORT=3000
# SESSION_SECRET=سلسلة-عشوائية-طويلة-لجلسات-لوحة-التحكم

# MariaDB (عند النشر على السحابة مع MariaDB من لوحة LibyanSpider)
# MARIADB_HOST=عنوان-خادم-ماريا
# MARIADB_PORT=3306
# MARIADB_USER=المستخدم
# MARIADB_PASSWORD=كلمة-المرور
# MARIADB_DATABASE=shop

# أو صيغة URL واحدة:
# DATABASE_URL=mysql://user:password@host:3306/shop

# الحماية (CORS) — مسارات API
# ALLOWED_ORIGINS=https://store.example.com,https://app.example.com
